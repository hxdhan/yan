<?php
usleep(5000000);
echo "tieer";
//error_log(json_encode($_POST));
//echo base64_encode(md5('tieer'));
//echo md5('tieer');
//phpinfo();

//echo 1;
//echo strtotime('1901-12-15');
// $from_path = 'old.jpg';
// $to_path = 'new.jpg';

// half_image($from_path, $to_path);

// function half_image($from_path, $to_path) {
	// $percent = 0.5;
	// list($width, $height) = getimagesize($from_path);
	// $newwidth = $width * $percent;
	// $newheight = $height * $percent;
	// $thumb = imagecreatetruecolor($newwidth, $newheight);
	// $source = imagecreatefromjpeg($from_path);
	// imagecopyresized($thumb, $source, 0, 0, 0, 0, $newwidth, $newheight, $width, $height);
	// imagejpeg($thumb,$to_path);
// }
 